# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:# ERPPortal — Coding Standards & Conventions

This document captures the conventions established during Phase 1 (Foundation).
Anyone adding a new feature should follow these patterns for consistency.

## 1. Project Structure — Clean/Onion Architecture

The backend is split into six projects, with dependencies only pointing inward:
